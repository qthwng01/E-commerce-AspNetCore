@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Register";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}

<div style="margin-top:16rem">

    <p id="external-login-description" class="text-info" style="text-align: center; font-size: 16px;">
        Bạn đã xác thực thành công với <strong>@Model.ProviderDisplayName
         </strong>. Vui lòng nhập địa chỉ email cho tài khoản và nhấn vào nút Xác nhận để hoàn tất quá trình đăng nhập.
    </p>

            <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post" style="text-align: center; margin-top: 2rem;">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Input.Email" style="font-size: 13px; font-weight: 700;">Email: </label>
                    <input asp-for="Input.Email" class="form-control" style=" width: 190px; border: 1px solid #989898; border-radius: 0.25rem; padding: 8px;" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                    <button type="submit" class="btn btn-primary" style=" border: 1px solid #1a73e8;
                                                                    border-radius: 5px; color: #fff; background: #1a73e8;
                                                                    padding: 6px; cursor: pointer; font-weight: bold;">Xác nhận</button>
                </div>
                
            </form>

    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
</div>
